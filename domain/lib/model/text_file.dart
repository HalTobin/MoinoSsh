class TextFile {
    final String name;
    final bool isEditable;
    final String content;

    const TextFile({
        required this.name,
        required this.isEditable,
        required this.content
    });
}
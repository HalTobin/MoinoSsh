name: data

environment:
  sdk: ^3.9.2
  flutter:

dependencies:
  flutter:
    sdk: flutter

  shared_preferences:
  path_provider:
  dartssh2:
  drift:
  drift_flutter:
  biometric_storage:
  json_serializable:
  collection:

  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

  drift_dev:
  build_runner:

resolution:
  workspace

flutter:
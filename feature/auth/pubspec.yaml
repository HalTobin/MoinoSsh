name: feature_auth

environment:
  sdk: ^3.9.2
  flutter:

dependencies:
  flutter:
    sdk: flutter

  lucide_icons_flutter:
  provider:
  domain:
    path: ../../domain
  ui:
    path: ../../ui
  util:
    path: ../../util

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

resolution:
  workspace

flutter: